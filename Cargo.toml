[package]
name = "crfsuite"
version = "0.3.3"
authors = ["Thibaut Lorrain <thibaut.lorrain@snips.ai>"]
edition = "2018"

[dependencies]
failure = "0.1"
libc = "0.2"
crfsuite-sys = { path = "crfsuite-sys" }

[workspace]

