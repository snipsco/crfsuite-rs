[package]
name = "crfsuite-sys"
version = "0.2.0-pre"
authors = ["Thibaut Lorrain <thibaut.lorrain@snips.ai>"]
build = "build.rs"

[build-dependencies]
gcc = "0.3"
bindgen = "0.25"

[dependencies]
libc = "0.2"

