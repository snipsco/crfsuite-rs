[package]
name = "crfsuite-sys"
version = "0.2.0-pre"
authors = ["Thibaut Lorrain <thibaut.lorrain@snips.ai>"]
build = "build.rs"

[build-dependencies]
bindgen = "^0.33"
gcc = "0.3"
dinghy-build = { version = "0.3", features = [ "with-bindgen" ] }

[dependencies]
libc = "0.2"
