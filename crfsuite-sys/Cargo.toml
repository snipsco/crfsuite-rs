[package]
name = "crfsuite-sys"
version = "0.2.0-pre"
authors = ["Thibaut Lorrain <thibaut.lorrain@snips.ai>"]
edition = "2018"
links = "crfsuite"

[build-dependencies]
gcc = "0.3"
bindgen = "^0.37"
dinghy-build = { version = "0.3", features = [ "with-bindgen" ] }

[dependencies]
libc = "0.2"
